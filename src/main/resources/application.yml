spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mukpick?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # 개발단계라 update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    property-naming-strategy: SNAKE_CASE

server:
  port: 8080

jwt:
  secret: 003f384f-c922-4c1e-8a77-a4d99f81bd52
  access-token-expiration: 15 # 15분
  refresh-token-expiration: 10080 # 7일

logging:
  level:
    org.hibernate.SQL: debug

kakao:
  client-id: 1e167faa0863e83ca396db9437e3bf6a
  redirect-uri: http://localhost:8080/callback
